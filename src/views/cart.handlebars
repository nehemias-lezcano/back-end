{{#each cartData}}
  <h2>{{product.title}}</h2>
  <p>description:{{product.description}}</p>
  <p> $:{{product.price}}</p>
  <p> code:{{code}}</p>
  <p> {{thumbnails}}</p>
  <p> ID:{{product._id}}</p>
  <p> quantity:{{quantity}}</p>
{{/each}}
